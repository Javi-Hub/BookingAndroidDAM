<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="rodrigo.javier.booking">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Booking"
        android:usesCleartextTraffic="true">
        <activity android:name=".SplashEndActivity"></activity>
        <activity android:name=".detailRoom.view.DetailRoomActivity" />
        <activity android:name=".login.view.LoginActivity" />
        <activity android:name=".lstRoom.view.LstRoomActivity" />
        <activity android:name=".fieldSearch.view.FieldSearchActivity" />
        <activity android:name=".bookedRooms.view.BookedRoomActivity" />
        <activity android:name=".filter.view.FilterHotelActivity" />
        <activity android:name=".detailHotel.view.DetailHotelActivity" />
        <activity android:name=".SearchActivity"/>
        <activity android:name=".lstHotel.view.LstHotelActivity" />
        <activity android:name=".SplashScreenActivity" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>